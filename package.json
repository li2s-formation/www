{
  "scripts": {
    "dev": "slidev",
    "build": "slidev build",
    "deploy": "npm run build && npx gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  },
  "dependencies": {
    "vite": "^7.2.2"
  }
}
